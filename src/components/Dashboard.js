import React, { useEffect } from "react";
// previous cohort applications
// each item labeled with title of cohort
// The cohort information should come from the firebase storage and stored in redux
const Dashboard = () => {

   return (
       <ul>
           <li>
               
           </li>
       </ul>
   )
}

export default Dashboard;